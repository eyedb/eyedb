# ATTENTION: the order is important!!!
# share/ must be built first because it contains the
# initialization of the database used by the other tests
SUBDIRS=share admin query store
DIST_SUBDIRS=share admin methods query store

